# Where this actually is

Kept current as work finishes, not as errors are noticed. This section has two
authors: code work is known the moment it lands, but infrastructure work —
a token regenerated, a migration run, a workflow enabled — happens in dashboards
this file cannot see, and only enters it when someone says so.

Last updated: 2026-08-27.

## Built and proven against real data

Stages 0 through 6. The app is deployed, the database holds **246 posts** (every
one with a `latest` insight), **696 days** of account metrics, and the three
surfaces — chat, dashboard, calendar — all work against it.

34 commits · 67 source files · ~9,000 lines · 157 tests in 12 files · 4 migrations.

## Pending migrations

`npm run db:migrate` before the next deploy. Two columns are added and nothing
reads correctly without them:

- `account_daily.followers_total` — the only column a net follower change can be
  computed from
- `model_runs.calls` — requests per row; the quota guard reads it

`/settings` says **"cannot read"** against the request counts if this has not
been run. That is the check.

## The one with a deadline

**Nothing refreshes the Instagram access token.** It had 56 days remaining when
it was regenerated during Task 2.1, nothing has extended it since, and
`token.yml` — the weekly refresh the plan calls for — was never written.

When it expires the daily sync starts failing and **nothing announces it**: the
expiry is visible only by running `npm run probe:graph` by hand. `/settings`
shows the scopes but not the expiry date.

Run `npm run probe:graph` now to see how long is left.

## Planned, not built

| Item                                   | Where it was specified          | Consequence today                                                        |
| -------------------------------------- | ------------------------------- | ------------------------------------------------------------------------ |
| `token.yml` — weekly token refresh     | Plan A3                         | The token expires and the sync stops, silently                           |
| `insights.yml` — daily card generation | Plan A3                         | Dashboard notes are only ever generated by the Refresh button            |
| Comments sync unit                     | Plan A2                         | `post_comments` exists and stays empty; no `getComments` tool            |
| `getCalendarEntries` chat tool         | Plan B1, explicitly for Stage 4 | The chat cannot answer "what's due this week"                            |
| One repair attempt before dropping     | Plan A5                         | An unbacked figure is dropped without asking the model to correct itself |
| Backup restore, performed once         | Plan Task 3.2                   | An untested backup is not a backup                                       |
| Live smoke suite after deploy          | Plan B1 test plan, Part E       | The gap that let a broken stylesheet ship green                          |

## Open questions

| #   | Question                                                 | Settles by                                                                        |
| --- | -------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Q2a | Do `FOLLOWER` / `NON_FOLLOWER` mean follows / unfollows? | Recording `followers_count` on 2026-09-02 and re-running `probe:account-insights` |
| —   | The chat acceptance test has never been graded           | Asking it, twice, per plan B1                                                     |

## Quota, as observed

`google:gemini-3.6-flash`, free tier: **5 requests/minute, 20/day**, 250K
tokens/minute. A question costs up to 4 requests, so a day is about 4 questions.
See `docs/quota.md`. Switching provider is an environment change — `groq`,
`deepseek`, `openrouter`, `together`, `cerebras` and `mistral` are registered.
